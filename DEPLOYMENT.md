# دليل النشر الكامل — من المشروع الجاهز إلى موقع حي 100%

هذا الدليل يفترض أنك تبدأ من صفر حسابات. كل خطوة فيها روابط مباشرة. الوقت
الإجمالي المتوقع: **20-40 دقيقة**.

---

## 1) رفع المشروع على GitHub

1. أنشئ حساب على https://github.com (إن لم يوجد).
2. أنشئ مستودع (Repository) جديد فارغ، مثلاً `consul-website`.
3. من داخل مجلد المشروع:
   ```bash
   git init
   git add .
   git commit -m "CONSUL website — initial version"
   git branch -M main
   git remote add origin https://github.com/<username>/consul-website.git
   git push -u origin main
   ```

---

## 2) النشر على Vercel (الاستضافة + CDN + SSL تلقائياً)

1. أنشئ حساب على https://vercel.com وسجّل الدخول بحساب GitHub.
2. اضغط **Add New → Project** واختر مستودع `consul-website`.
3. Vercel يكتشف Next.js تلقائياً — لا تغيّر أي إعداد بناء.
4. لا تضغط Deploy بعد — أولاً أضف متغيرات البيئة (Environment Variables) من
   ملف `.env.example`، حتى لو تركتها فارغة مبدئياً (الموقع سيعمل بوضع
   Placeholder). كحد أدنى أضف:
   - `ADMIN_PASSWORD` — كلمة مرور قوية للوحة التحكم
   - `NEXT_PUBLIC_SITE_URL` — سيتوفر بعد أول نشر (عدّله لاحقاً لدومينك النهائي)
5. اضغط **Deploy**. بعد دقيقة تقريباً يصبح عندك رابط مباشر مثل
   `consul-website.vercel.app` — **يعمل بـ SSL تلقائياً** (شهادة أمان مجانية
   من Vercel).

---

## 3) ربط الدومين الخاص بالشركة

1. اشترِ الدومين إن لم يوجد (مثال: عبر GoDaddy أو Namecheap أو أي مزوّد محلي).
2. في مشروع Vercel: **Settings → Domains → Add**، أدخل الدومين (مثل
   `consul-bags.com`).
3. Vercel يعطيك سجلات DNS (عادة `A` أو `CNAME`) — أضفها في لوحة تحكم الدومين
   عند مزوّد الدومين.
4. بعد انتشار DNS (من دقائق إلى بضع ساعات)، يعمل الدومين مع **SSL تلقائي**
   بدون أي خطوة إضافية منك.
5. حدّث `NEXT_PUBLIC_SITE_URL` في إعدادات المتغيرات على Vercel إلى الدومين
   الجديد، ثم أعد النشر (Redeploy).

---

## 4) تفعيل Supabase (قاعدة بيانات حقيقية — الحفظ الدائم)

بدون هذه الخطوة، لوحة التحكم تعمل لكن التعديلات لا تبقى دائماً (وضع
Placeholder). بعد هذه الخطوة تصبح كل الإضافات/التعديلات محفوظة بشكل دائم.

1. أنشئ حساب على https://supabase.com → **New Project**.
2. من **Project Settings → API** انسخ:
   - `Project URL` → ضعه في `NEXT_PUBLIC_SUPABASE_URL`
   - `anon public key` → ضعه في `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `service_role key` → ضعه في `SUPABASE_SERVICE_ROLE_KEY` (سرّي، لا يُنشر)
3. من **SQL Editor** داخل Supabase، الصق محتوى الملف
   `src/lib/supabase/schema.sql` وشغّله (Run). هذا ينشئ الجداول الثلاثة
   (`categories`, `catalogs`, `catalog_images`) مع صلاحيات القراءة العامة
   والكتابة للمسجّلين فقط.
4. من **Storage** أنشئ 3 Buckets عامة (Public):
   `catalog-covers`, `catalog-images`, `catalog-pdfs`.
5. من **Authentication → Users** أنشئ مستخدم إداري واحد (بريدك) لتسجيل
   الدخول للوحة التحكم لاحقاً.
6. أضف المتغيرات الثلاثة في Vercel (**Settings → Environment Variables**)
   وأعد النشر.
7. أخبرني بعدها إن احتجت تحويل `src/lib/data.ts` فعلياً من القراءة من ملف
   JSON المحلي إلى استعلامات Supabase — الدوال والتوقيعات جاهزة لذلك، وهي
   نقطة التحويل الوحيدة في المشروع.

---

## 5) تفعيل Cloudinary (رفع وتحسين الصور من لوحة التحكم)

1. أنشئ حساب على https://cloudinary.com (الخطة المجانية كافية للبداية).
2. من **Dashboard** انسخ `Cloud Name`، `API Key`، `API Secret` وضعها في
   `.env` المطابقة في Vercel.
3. بعد هذا، حقل "رابط صورة الغلاف" في لوحة التحكم يمكن استبداله بزر رفع
   مباشر (upload widget) — هذا تطوير بسيط إضافي بمجرد جاهزية الحساب.

---

## 6) اختبار ما قبل التسليم النهائي

- [ ] الموقع يفتح على الدومين النهائي بـ HTTPS (قفل أخضر في المتصفح)
- [ ] التبديل بين العربي/الإنجليزي يعمل ويعكس اتجاه الصفحة (RTL/LTR)
- [ ] فتح كاتالوج، والتنقل بالسحب (swipe) ولوحة المفاتيح، والتكبير، وملء الشاشة
- [ ] زر المشاركة: نسخ الرابط، واتساب، رمز QR
- [ ] لوحة التحكم: تسجيل الدخول، إضافة كاتالوج، حذفه
- [ ] الموقع يعمل بسلاسة على الجوال (Mobile) و RTL على الجوال بالذات
- [ ] `robots.txt` و `sitemap.xml` يفتحان على `/robots.txt` و `/sitemap.xml`

---

## ما بعد التسليم

هذا المشروع جاهز للتوسّع: صفحات فئات مستقلة، رفع PDF فعلي لكل كاتالوج، تعدد
مندوبين مع صلاحيات، تحليلات مشاهدة لكل رابط، تطبيق لغات إضافية (البنية تدعم
ذلك في `src/i18n/`). أي توسعة من هذه يمكن بناؤها لاحقاً بدون إعادة هيكلة
المشروع.
