import { notFound } from "next/navigation";
import { Building2, MapPin } from "lucide-react";
import type { Locale } from "@/i18n/config";
import { getCatalogsByWarehouse, getCategories, getSiteSettings, getWarehouseBySlug } from "@/lib/data";
import { getDictionary } from "@/i18n/getDictionary";
import CatalogGrid from "@/components/CatalogGrid";
export default async function WarehousePage({params}:{params:Promise<{locale:Locale;slug:string}>}){const {locale,slug}=await params;const warehouse=await getWarehouseBySlug(slug);if(!warehouse)notFound();const [catalogs,categories,dict,settings]=await Promise.all([getCatalogsByWarehouse(warehouse.id),getCategories(),getDictionary(locale),getSiteSettings()]);const ar=locale==="ar";return <><section className="relative overflow-hidden border-b border-(--color-line) px-5 py-24 md:px-8"><div className="hero-grid absolute inset-0 opacity-30"/><div className="relative mx-auto max-w-7xl"><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-(--color-gold)/20 bg-(--color-gold)/8 px-3 py-1.5 text-xs text-(--color-gold)"><Building2 size={14}/>{ar?"مستودع CONSUL":"CONSUL Warehouse"}</div><h1 className="font-(family-name:--font-display) text-5xl md:text-7xl">{ar?warehouse.nameAr:warehouse.nameEn}</h1><p className="mt-5 flex items-center gap-2 text-sm text-(--color-bone)"><MapPin size={15} className="text-(--color-gold)"/>{ar?warehouse.cityAr:warehouse.cityEn} · {catalogs.length} {ar?"كتالوج":"catalogs"}</p>{(ar?warehouse.descriptionAr:warehouse.descriptionEn)&&<p className="mt-5 max-w-xl text-sm leading-7 text-(--color-smoke)">{ar?warehouse.descriptionAr:warehouse.descriptionEn}</p>}</div></section><CatalogGrid catalogs={catalogs} categories={categories} locale={locale} dict={dict} description={ar?settings.libraryDescriptionAr:settings.libraryDescriptionEn}/></>}
